//! Stateful components composed from primitive templates.

pub mod bar_buttons;
pub mod bar_container;
/// Reusable calendar grid widget with month navigation and day selection.
pub mod calendar;
/// Shared credential box (clock, date, secret entry) for lock screen + greeter.
pub mod credential_box;
